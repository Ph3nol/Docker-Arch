name: Docker Arch
services:
    # PHP Container
  - type: php
    path: ../
    container:
        packages:
            - make
    options:
        version: "7.1"
        cli_only: true
        zsh: true
        custom_zsh: true
        dotfiles: true
        composer: true
        config:
            phar.readonly: 0
