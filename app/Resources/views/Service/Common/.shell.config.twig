{% set service = generator.service %}
echo ""
echo "Welcome to {{ service.identifier }} container!"
echo "This container has been generated by {{ constant('\\Ph3\\DockerArch\\Application\\Architect::TYPE_PROJECT_NAME') }} open source project."
echo ""
echo "Don't update this file."
echo "To make changes, update your project .{{ constant('\\Ph3\\DockerArch\\Application\\Architect::TYPE_PROJECT_CONFIG_FILENAME') }} file,"
echo "and generate your new Docker configuration."
echo ""
echo "Feel free to Share/Watch/Star/Fork on Github: {{ constant('\\Ph3\\DockerArch\\Application\\Architect::TYPE_PROJECT_URL') }}."
echo ""
echo "Enjoy! 🐳"
echo ""

{% if service.zsh and service.customZsh %}
# --- Antigen

source /root/antigen.zsh

antigen use oh-my-zsh

antigen bundle git
antigen bundle brew
antigen bundle common-aliases
antigen bundle git-extras
antigen bundle git-flow
antigen bundle osx
antigen bundle web-search
antigen bundle docker
antigen bundle docker-compose
antigen bundle zsh-users/zsh-syntax-highlighting
antigen bundle zsh-users/zsh-history-substring-search ./zsh-history-substring-search.zsh
antigen bundle command-not-found

antigen theme robbyrussell

# Only for Mac with Touch Bar
antigen theme https://github.com/iam4x/zsh-iterm-touchbar

antigen apply

{% endif %}
### --- Exports

export EDITOR="vim"
export GIT_EDITOR="vim"
{% if service.zsh and service.customZsh %}
export PROMPT='${ret_status} (🐳  {{ service.identifier }}) %{$fg[cyan]%}%c%{$reset_color%} $(git_prompt_info)'
{% endif %}

### --- Aliases

# @todo

### --- SSH

eval `ssh-agent -s` &> /dev/null
ssh-add $(find /root/.ssh -maxdepth 1 -type f -printf '%p\n' | grep id_) &> /dev/null
